def nyc_pigeon_organizer(data)
  pigeons = {}
  
  data.each { |attribute_name, attribute|
    attribute.each { |string, names|
      names.each { |name|
        pigeons[name] || = {}
        pigeons[color_gender_place] || = []
        
  
  
  
end 